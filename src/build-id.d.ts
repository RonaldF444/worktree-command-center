// Injected by esbuild (`define` in esbuild.config.mjs) into the desktop renderer and the browser
// bundle: the build's id, shared by both so a stale browser can detect an updated server.
declare const __WCC_BUILD__: string;
